source "https://rubygems.org"

gem 'rake'

gem 'haml' #haml
gem 'sass' #sass

gem 'sinatra'

gem 'require_all' #to include all the models in one line


gem 'data_mapper'
gem 'dm-core'
gem 'dm-timestamps'

#Sqlite
gem 'dm-sqlite-adapter'
#or Myqsl
#gem 'dm-mysql-adapter'
    #on windows gem install do_mysql  -- --with-mysql-dir=C:/paht_to/mysql-connector-c (downloadable at http://dev.mysql.com/downloads/connector/c/) 
    #For use with bundle do 'bundle config build.do_mysql -- --with-mysql-dir=C:/paht_to/mysql-connector-c'


gem 'shotgun'   #Automatic code reloading for rack
                 #But not working for windows =( But who use windows to dev
